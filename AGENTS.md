# AGENTS.md

## 适用范围

本文件适用于仓库根目录及其所有子目录。

本仓库维护 Surge 与 Shadowrocket 可远程引用的纯文本分流规则。默认任务是维护 `.list` 规则与相关文档，不要擅自加入节点、订阅、证书、凭据、完整客户端配置或自动更新服务。

## 仓库结构

- `Base/`：必须优先匹配的局域网规则和基础直连白名单。
- `China/`：主要面向中国大陆网络或服务的规则。
- `Proxy/`：通常需要代理、地区节点或专用策略组的规则。
- `README.md`：面向使用者的仓库说明与接入示例。
- `LICENSE`：仓库许可证。

二级目录按业务职责拆分。新增规则时优先放入现有分类；只有在职责明确、规则数量足以独立维护时才新建文件或目录，避免为单条规则制造新分类。

目录名只表达主要路由意图。遇到金融服务、流媒体解锁或跨区服务时，以文件头的“建议策略”和实际业务需求为准，不得仅根据 `China/` 或 `Proxy/` 目录名机械判断策略。

## 文件约定

规则文件统一使用 `.list` 扩展名、UTF-8 编码和 LF 换行。

每个文件必须以以下三项中文说明开头，随后保留一个空行：

```text
# 名称：规则集名称
# 用途：匹配对象与维护边界。
# 建议策略：客户端中的推荐路由方式。
```

正文按业务含义分组。每组使用 `# 分组：...` 作为标题，并在不同分组之间保留空行。注释应说明规则的业务归属或维护原因，不要逐行复述规则文本。

一行只写一条规则，格式为 `TYPE,VALUE[,OPTION]`。仓库当前允许的类型包括：

- `DOMAIN`、`DOMAIN-SUFFIX`、`DOMAIN-KEYWORD`
- `IP-CIDR`、`IP-CIDR6`、`IP-ASN`、`GEOIP`
- `USER-AGENT`、`PROCESS-NAME`
- `URL-REGEX`、`OR`

新增其他类型前，必须确认 Surge 与目标 Shadowrocket 版本的支持情况，并同步更新 `README.md` 的兼容性说明。

## 编辑原则

- 域名及域名关键字统一使用小写，除非客户端匹配语义明确要求保留大小写。
- IP 规则必须使用合法 CIDR。无需 DNS 解析的 `IP-CIDR`、`IP-CIDR6`、`IP-ASN` 与 `GEOIP` 规则应保留 `no-resolve`。
- 不在规则集中附加 `DIRECT`、`REJECT` 或策略组名称；策略由引用方的 `RULE-SET` 决定。
- 同一逻辑分组内优先按规则类型和字典序组织，但不得为了排序改变有先后语义的规则。
- 新增前检查当前文件、同业务文件及上位分类，避免重复或被更宽泛规则完全覆盖。
- 共享 CDN、云平台和身份验证域名可能服务多个产品。只有在依赖关系明确时才加入业务专用规则，避免扩大误匹配范围。
- 注释等内容使用中文标点符号。
- 数字、字母和中文之间要留有一个空格，提高可读性。
- 不要顺手重排或格式化无关的大型规则文件。变更应聚焦任务目标，并让差异可审查。
- 目录结构、接入方式、支持的规则类型或重要策略建议发生变化时，同步修改 `README.md`。

## 新增与拆分规则

新增域名时，优先选择能准确覆盖需求的最窄规则：

1. 仅匹配单一主机时使用 `DOMAIN`。
2. 确认整个域及其子域属于同一策略时使用 `DOMAIN-SUFFIX`。
3. 只有域名结构不稳定且误匹配风险可接受时使用 `DOMAIN-KEYWORD`，并同时满足下文的附加约束。
4. 仅在域名规则无法覆盖真实流量时补充 IP、ASN、进程或正则规则。

`DOMAIN-KEYWORD` 必须谨慎使用，默认不选它。它对主机名做子串匹配，选词稍宽就会静默劫持无关域名，而这类误伤在客户端的规则命中记录里极难察觉。

新增前必须同时满足以下条件：

- 精确规则确实覆盖不了目标流量。`DOMAIN` 与 `DOMAIN-SUFFIX` 已能覆盖时，不得再加关键字兜底，那只是冗余。
- 已实测误伤面。枚举含该关键字的其他注册域，逐个确认归属与 IP 落点，确保没有活跃的无关站点被卷入。
- 关键字足够独特。不要使用短品牌词、通用拼音串或常见英文单词。
- 与该文件的建议策略方向相容。`DIRECT` 规则集尤其危险：误伤境外域名会强制直连，往往直接导致无法访问；走代理的规则集误伤通常只是绕路。

已注册但当前没有解析的自有域名，应在确认归属后显式收录，不要交给关键字兜底。归属可用权威 NS 与已确认域名比对判定。

`DOMAIN-KEYWORD` 只匹配主机名，不匹配路径与查询参数，因此不要用它匹配 URL 片段。

拆分或移动规则时，应同时检查原文件与目标文件的用途和建议策略。不要仅复制规则后留在两处；确需跨策略重复时，要用分组注释说明原因。

## 校验要求

仓库没有构建流程。完成修改后至少执行以下只读或无副作用检查：

```sh
git diff --check

rg -n '[[:blank:]]+$' Base China Proxy README.md AGENTS.md

rg -n -v '^(#.*|$|(DOMAIN|DOMAIN-SUFFIX|DOMAIN-KEYWORD|IP-CIDR|IP-CIDR6|IP-ASN|GEOIP|USER-AGENT|PROCESS-NAME|URL-REGEX|OR),.+)$' \
  Base China Proxy \
  -g '*.list'
```

前两条命令应无输出。第三条命令用于发现空行、注释和已知规则类型之外的异常内容，正常情况下也应无输出。

如修改了规则文件，还应检查单文件重复项：

```sh
for file in $(find Base China Proxy -type f -name '*.list' | sort); do
  awk -v file="$file" \
    'NF && $0 !~ /^#/ { seen[$0]++; if (seen[$0] == 2) print file ": duplicate: " $0 }' \
    "$file"
done
```

最后查看 `git diff`，重点确认：

- 规则是否放入正确分类，策略建议是否准确。
- 是否出现重复、过宽匹配或规则顺序回归。
- 文档示例中的路径与仓库实际路径是否一致。

若客户端可用，应在 Surge 或 Shadowrocket 中刷新受影响的远程规则，并通过规则命中记录验证预期流量。无法进行客户端验证时，需要在交付说明中明确指出。

## 安全边界

- 不提交代理节点、订阅链接、账号、令牌、Cookie、证书或其他敏感信息。
- 不通过修改规则绕过用户未明确授权的网络或访问控制。
- 不自动抓取并整体覆盖现有规则；批量更新必须是明确任务，并经过人工审查。
- 不把未经验证的第三方列表直接并入仓库。
